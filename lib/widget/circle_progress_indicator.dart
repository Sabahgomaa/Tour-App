
import 'package:flutter/material.dart';
import 'package:tour_app/const/color.dart';

class CircileProgress extends StatelessWidget {
  const CircileProgress({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator(color: Dahab,);
  }
}
